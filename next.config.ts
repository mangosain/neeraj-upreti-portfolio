import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// next.config.js
module.exports = {
  images: {
    domains: ['mohd-shahid-iqbal.github.io'], // Add the external domain here
  },
};


export default nextConfig;
